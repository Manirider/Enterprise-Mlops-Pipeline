# ============================================================
# Enterprise MLOps Pipeline — Python Dependencies
# ============================================================
# Pin ranges are intentionally permissive to allow DVC
# environment resolution while guaranteeing compatibility.
# Run: pip install -r requirements.txt
# ============================================================

# ── Core ML Stack ──────────────────────────────────────────
scikit-learn>=1.3.0,<2.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
joblib>=1.3.0,<2.0.0

# ── MLOps Orchestration ────────────────────────────────────
dvc[gdrive]>=3.30.0,<4.0.0

# ── Configuration ──────────────────────────────────────────
PyYAML>=6.0.0,<7.0.0

# ── Testing ────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0

# ── Visualization (benchmarks & reports) ───────────────────
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# ── Jupyter (EDA notebook) ─────────────────────────────────
jupyter>=1.0.0,<2.0.0
ipykernel>=6.25.0,<7.0.0

# ── Development Utilities ──────────────────────────────────
# Optional: uncomment to enable type checking and linting
# mypy>=1.5.0
# ruff>=0.1.0
# black>=23.0.0
